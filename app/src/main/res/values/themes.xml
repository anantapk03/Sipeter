<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Sipeter" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorOnBackground">@color/windowBackground</item>

    </style>

    <style name="Theme.Sipeter" parent="Base.Theme.Sipeter" />
</resources>